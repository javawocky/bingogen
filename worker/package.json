{
  "name": "motobingo-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy:test": "wrangler deploy --env test",
    "deploy:prod": "wrangler deploy --env prod",
    "kv:create:test": "wrangler kv namespace create KV --env test",
    "kv:create:prod": "wrangler kv namespace create KV --env prod"
  },
  "devDependencies": {
    "wrangler": "^3.108.1",
    "@cloudflare/workers-types": "^4.20250424.0",
    "typescript": "^5.9.3"
  }
}
