services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: motobingo
      POSTGRES_USER: motobingo
      POSTGRES_PASSWORD: motobingo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
